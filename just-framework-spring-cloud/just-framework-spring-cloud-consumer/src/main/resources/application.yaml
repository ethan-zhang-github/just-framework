server:
  port: 8070
spring:
  application:
    name: consumer
feign:
  okhttp:
    enabled: true
  client:
    config:
      userClient:
        connectTimeout: 60
        readTimeout: 60
userClient:
  ribbon:
    listOfServers: localhost:8090